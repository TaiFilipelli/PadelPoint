export default function EditRacket() {
    return (
        <section>
            <h1>Aquí editamos las paletas, habrá un método desde la API con su respectivo endpoint</h1>
        </section>
    );
}