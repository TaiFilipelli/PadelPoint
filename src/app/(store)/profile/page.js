export default function ProfilePage() {
    return (
        <section>
            <h1>Esto será el perfil de usuario. Se accederá mediante la navbar, en el botón renderizado nuevo.</h1>
        </section>
    );
}